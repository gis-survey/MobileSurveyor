<?xml version="1.0" encoding="utf-8"?>
<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android">
    <EditTextPreference
        android:key="base_url"
        android:title="API base url" />
    <EditTextPreference
        android:key="map_rtes"
        android:hint="must be comma separated route ids"
        android:title="Route IDs for Map"/>
</PreferenceScreen>